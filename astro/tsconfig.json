{
  "extends": "astro/tsconfigs/strictest",
  "compilerOptions": {
    "baseUrl": "src",
    "exactOptionalPropertyTypes": true,
    "paths": {
      "$component/*": ["component/*"],
      "$lib/*": ["lib/*"],
      "$layout/*": ["layout/*"]
    }
  }
}
